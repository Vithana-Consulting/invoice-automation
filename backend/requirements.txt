# Web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Database
sqlalchemy>=2.0.0
pymysql>=1.1.0
alembic>=1.13.0

# Configuration
pydantic-settings>=2.1.0
python-dotenv>=1.0.0

# LLM parser (OpenAI SDK — also used for Gemini/Ollama OpenAI-compatible endpoints)
openai>=1.0.0

# PDF / OCR parsing
pdfplumber>=0.10.0
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0

# ── System binaries (NOT pip-installable — installed via apt in backend/Dockerfile) ──
#   tesseract-ocr      → OCR engine for pytesseract
#   poppler-utils      → PDF→image rasterising for pdf2image
#   libreoffice-writer → .doc/.docx → PDF conversion (app/utils/document_converter.py)
# Non-Docker hosts must install these manually, e.g.:
#   apt-get install -y tesseract-ocr poppler-utils libreoffice-writer   (Debian/Ubuntu)
#   brew install tesseract poppler && brew install --cask libreoffice    (macOS)

# Gmail API
google-api-python-client>=2.100.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0

# S3 file storage backend (STORAGE_BACKEND=s3) — uses the default boto3
# credential chain (IAM instance role on AWS App Runner/EC2/ECS; env vars
# or ~/.aws/credentials for local dev). No access-key config fields needed.
boto3>=1.34.0

# File upload support
python-multipart>=0.0.7

# HTTP client (for Zoho API)
httpx>=0.27.0

# Retry logic
tenacity>=8.2.0

# LlamaParse (optional)
llama-parse>=0.4.0

# Auth - OAuth + JWT
authlib>=1.3.0
pyjwt>=2.8.0
cryptography>=42.0.0

# Excel export
openpyxl>=3.1.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
